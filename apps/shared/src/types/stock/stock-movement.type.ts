export enum StockMovementType {
    PURCHASE = 'purchase',
    USAGE = 'usage',
    WASTE = 'waste',
    ADJUSTMENT = 'adjustment',
    RETURN = 'return',
    TRANSFER = 'transfer' // Between restaurants
}
